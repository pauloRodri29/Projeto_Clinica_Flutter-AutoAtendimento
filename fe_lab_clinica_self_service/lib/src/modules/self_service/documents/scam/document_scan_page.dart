import 'package:flutter/material.dart';

class DocumentScanPage extends StatelessWidget {

  const DocumentScanPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Document Scam'),),
           body: Container(),
       );
  }
}