import 'package:flutter/material.dart';

class DocumentsScamConfirmPage extends StatelessWidget {

  const DocumentsScamConfirmPage({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Document Scam Confirm'),),
           body: Container(),
       );
  }
}